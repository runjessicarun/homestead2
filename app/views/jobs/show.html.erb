

<div class="jumbotron">
  <h1><%= @job.title %> </h1>
  <p><%= simple_format(@job.description) %></p>
  <p>
    <button class="btn btn-default" type="submit">
    <div class="text-left">
    <%= link_to("投交履历",  new_job_resume_path(@job), :style => "font-size: 30px") %>
  </div>
</button>
</p>
</div>
